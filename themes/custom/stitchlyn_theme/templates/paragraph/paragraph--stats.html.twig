<section class="stats-section w-100 py-5" data-aos="fade-up">
  <div class="container">
    <div class="row justify-content-center text-center">
      {% for key, item in content.field_stats_item %}
        {% if key|first != '#' %}
          {{ item }}
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>