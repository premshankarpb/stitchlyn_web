<!DOCTYPE html>
<html lang="{{ language.getId }}">
  <head>
    <head-placeholder token="{{ placeholder_token|raw }}">
    <title>{{ head_title|safe_join(' | ') }}</title>
    <css-placeholder token="{{ placeholder_token|raw }}">
    <js-placeholder token="{{ placeholder_token|raw }}">
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  </head>
  <body{{ attributes.addClass(body_classes) }}>

    {% include "@stitchlyn_theme/layout/header.html.twig" %}

    <div class="admin-layout">
      <aside class="admin-sidebar dark-sidebar">
        {% include "@stitchlyn_theme/navigation/menu--backend.html.twig" with {
          backend_menu: page.backend_menu
        } %}
      </aside>

      <main class="admin-main">
        {% if messages %}
          <div class="messages">{{ messages }}</div>
        {% endif %}

        {{ page.content }}
      </main>

    {% include "@stitchlyn_theme/layout/footer--dashboard.html.twig" %}
    </div>

    <js-bottom-placeholder token="{{ placeholder_token|raw }}">
  </body>
</html>
